import React from 'react';

const Header = () => (
    <header>
        <h1>ABOUT US</h1>
        <h2>We want to create a fun environment that encourages poeple to get fit and through competitions so we can all better each others  lives.</h2>
    </header>
)

export default Header;